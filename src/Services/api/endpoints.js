import { AUTH, OAUTH } from "@/features/auth/api/endpoints";
import { PROFILE } from "@/features/profile/api/endpoints";
import { PROJECTS } from "@/features/projects/api/endpoints";
import { APPLICATIONS } from "@/features/applications/api/endpoints";
import { CONTRACTS } from "@/features/contracts/api/endpoints";
import { MILESTONES } from "@/features/milestones/api/endpoints";
import { SETTINGS } from "@/features/settings/api/endpoints";
import { ADMIN_VERIFICATION } from "@/features/admin/api/endpoints";
import { SUBMISSIONS } from "@/features/submissions/api/endpoints";
import { TEAMS } from "@/features/teams/api/endpoints";
import { VERIFICATION } from "@/features/verification/api/endpoints";

export const ENDPOINTS = {
  AUTH,
  OAUTH,
  PROFILE,
  PROJECTS,
  APPLICATIONS,
  CONTRACTS,
  MILESTONES,
  SETTINGS,
  ADMIN_VERIFICATION,
  SUBMISSIONS,
  TEAMS,
  VERIFICATION,
};

export default ENDPOINTS;